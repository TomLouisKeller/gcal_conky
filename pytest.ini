[pytest]
# don't show deprecation warnings we can't do anything about
# filterwarnings = ignore:.*:DeprecationWarning
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
